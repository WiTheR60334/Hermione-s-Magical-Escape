<%- include("partials/header", {isHotelPage : false}); -%>


<section class="destination" id="destination" style="margin-top: 100px;">


  <% if (user && user.role === 'admin') { %>
    <div class="admin-buttons">
      <a href="/alldestinations/<%= destination._id %>/edit" class="btn btn-primary">Edit Post</a>
      <form action="/alldestinations/<%= destination._id %>/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete this post?');">
        <button type="submit" class="btn btn-danger">Delete Post</button>
      </form>
    </div>
  <% } %>

  <div class="heading">
    <span>Destination Details</span>
    <h1 style="font-size: 5rem;"><%= destination.title %></h1>
  </div>

  <div class="swiper slider">
    <div class="swiper-wrapper">
        <div class="swiper-slide">
            <img src="<%=destination.firstImg%>" class="firstImg" alt="<%=destination.title%>" />
            <% destination.imagesURL.forEach(function(image){ %>
          </div>
          <div class="swiper-slide">
            <img src="<%=image%>" alt="<%=destination.title%>" />
            <% }); %>
          </div>
        </div>
    </div>
</div>

<div class="content3" data-aos="fade-up" data-aos-delay="150" style="text-align: center;margin-bottom: 6rem; margin-top: -2rem;">
  <a href="/flightsearch/<%=destination.title%>" class="btn">Book Now</a>
</div>


  <div class="content1"  data-aos="fade-up" data-aos-delay="150">
    <p><%= destination.firstPara %></p>
    <p><%= destination.content %></p>
  </div>

  <!-- <div class="images">
    <img src="<%=destination.firstImg%>" class="firstImg" alt="<%=destination.title%>" />

    <% destination.imagesURL.forEach(function(image){ %>
      <img src="<%=image%>" alt="<%=destination.title%>" />
    <% }); %>

  </div> -->
  <div class="tp5" data-aos="fade-up" data-aos-delay="150" style="margin-top: 8rem;">
    <a href="/alldestinations"><i class="fas fa-angle-left"></i>Back to All Destinations</a>
  </div>
  

</section>
<div class="space" style="margin-top: 20rem;"></div>


<%- include("partials/footer"); -%>
