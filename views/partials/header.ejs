<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Agency Website</title>

    <!-- font from cdnjs-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <link rel="stylesheet" href="/css/style.css">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">


    <script src="/js/script.js" defer></script>

</head>
<body>
    

    <header class="header">
        <a data-aos="zoom-in-left" data-aos-delay="150" href="#" class="logo"> <i class="fas fa-paper-plane"></i>Hermione's Magical Escape</a>
    
        <% if (user) { %>
          <div class="welcome" style="font-size: 3rem; color: var(--bordercolor);">
            <span>Welcome, <% if (user.googleDisplayName) { %><%= user.googleDisplayName %><% } else { %><%= user.name %><% } %></span>
          </div>
        <% }  %>
    
        
    
        <div class="icons">
            <form action="/search" class="search-form" method="post" style="margin-right: 50px;">
                <input type="search" name="searchTerm" placeholder="Search Destinations.." id="searchBox">
                <button type="submit" class="fas fa-search" id="search-btn" style="display: block; font-size : 30px;"></button>
            </form>
            <i class="bi bi-brightness-high-fill" id="toggleDark"></i>
            <% if (!user) { %>
              <div class="fas fa-user" id="login-btn"></div>
            <% } %>
            <div class="fas fa-bars" id="menu-btn" onclick="toggleMenu()"></div>
        </div>
    
        <nav class="navbar" id="navbar">
            <a data-aos="zoom-in-left" data-aos-delay="300" href="/">home</a>
            <a data-aos="zoom-in-left" data-aos-delay="300" href="/about">about</a>
            <a data-aos="zoom-in-left" data-aos-delay="450" href="/alldestinations">Destinations</a>
            <a data-aos="zoom-in-left" data-aos-delay="600" href="#pricing">pricing</a>
            <a data-aos="zoom-in-left" data-aos-delay="750" href="/services">services</a>
            <a data-aos="zoom-in-left" data-aos-delay="750" href="#review">review</a>
            <a data-aos="zoom-in-left" data-aos-delay="300" href="/contact">contact us</a>
            <% if(!user){ %>
            <a data-aos="zoom-in-left" data-aos-delay="800" href="/login">User Login</a>
            <% } %>
            <% if (user && user.role === 'admin') { %>
            <a data-aos="zoom-in-left" data-aos-delay="300" href="/newdestination">New Destination</a>
            <% } %>
            <% if (user && user.role === 'admin') { %>
                <a data-aos="zoom-in-left" data-aos-delay="300" href="/newflight">New Flight</a>
            <% } %>
            <% if(user) { %>
            <a href="/logout" class="fas fa-sign-out-alt">Logout</a>
           <% } %>
        </nav>
    
        <form action="/login" class="login-form" id="login-form" method="POST">
            <div class="inputBox">
                <span>Username</span>
                <input type="text" placeholder="enter your username" name="username">
            </div>

            <div class="inputBox">
                <span>Password</span>
                <input type="password" placeholder="enter your password" name="password">
            </div>

            <div class="remember">
                <input type="checkbox" name="" id="login-remember">
                <label for="login-remember">remember me</label>
            </div>

            <h3>Forgot Password ? <a href="#" class="forgot-password">Click Here</a></h3>


            <input type="submit" class="btn" value="login">
            <h3>Or continue with these social profile</h3>
            <div class="share">
                <a href="/auth/google" class="fab fa-google"></a>
                <a href="#" class="fab fa-facebook-f"></a>
                <a href="#" class="fab fa-twitter"></a>
            </div>
            <div class="end">
                    <a href="/signup" class="btn" id="register-btn">Register</a>     
            </div>

        </form>
    
        <!-- <a data-aos="zoom-in-left" data-aos-delay="900" href="#book-form" class="btn">book now</a> -->
    </header>
    
    <script>
        function toggleMenu() {
            var navbar = document.getElementById("navbar");
            navbar.classList.toggle("active");
        }
    </script>
    